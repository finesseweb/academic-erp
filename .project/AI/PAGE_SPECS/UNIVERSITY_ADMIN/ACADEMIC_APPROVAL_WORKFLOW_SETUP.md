## Common Inbox integration

Workflows with `Applies To = CURRICULUM` and `Applies To = ACADEMIC_POLICY` feed the same Academic Approval Inbox. Workflow setup controls who approves; the Inbox dynamically resolves the submitted subject type.
