# Intake Lifecycle Capability Unification — 2026-08-25

- Added dedicated `can.lifecycle` Inertia capability for Intake lifecycle rendering.
- UI no longer independently infers lifecycle visibility from possibly stale `can.enable/can.disable` values.
- Controller and status POST share the same protected lifecycle authorization function.
- Effective exact-College COLLEGE_ADMIN and effective SUPER_ADMIN are recognized.
- Normal explicit permissions remain valid.
- No seat-capacity business rule changed.
