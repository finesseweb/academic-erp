# college_application_fee_rules

Scoped Application Fee configuration with University/College ownership and optional Degree Level, Degree, Program Template, College Program Offering, and Admission Cycle selectors. `fee_required = false` means FREE. Resolver uses most-specific active rule; resolved rule/amount/currency are snapshotted on the Application.
