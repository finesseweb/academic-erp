# college_admission_application_field_values

Stores one dynamic field response per Admission Application + Form Field. Scalar values use `value_text`, multi-value responses use `value_json`, and file/image inputs store private storage path plus original filename, MIME and size metadata.
