# college_admission_form_steps

Ordered dynamic steps belonging to one Admission Form Template. `display_order` controls form sequence. `is_locked` is reserved for governed base-template protection/extension enforcement.
