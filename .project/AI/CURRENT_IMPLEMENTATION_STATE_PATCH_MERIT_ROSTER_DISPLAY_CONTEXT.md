# Current Implementation State — Merit/Roster display context

The Merit/Roster screen now keeps tie-break computation unchanged while rendering submission timestamps and DOB values in readable form. Academic context now explicitly shows Degree Level, Degree and exact bucket Discipline. Program-wide seat buckets display a non-fixed discipline scope.
